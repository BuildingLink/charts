questions:
  - variable: postgresDatabaseName
    label: "User"
    type: string
    default: "weblate"
    required: true
    description: "Specify the postgres user name for weblate"
    group: "Postgres Settings"

  - variable: postgresUserName
    label: "User"
    type: string
    default: "weblate"
    required: true
    description: "Specify the postgres user name for weblate"
    group: "Postgres Settings"
  - variable: postgresPassword
    label: "Password"
    type: password
    required: true
    description: "Specify password of the user"
    group: "global Settings"

  